# 技术设计：验收文档同步

## 1. 设计边界

这是一次纯文档一致性修复。`contracts/database.md`、`contracts/openapi.yaml`、迁移 `0033_task_owned_history_delete.py`、删除服务和集成测试共同提供已实现证据；其中 `contracts/database.md` 是数据库规则的权威可编辑来源。目标文档只引用该合同，不复制实现细节形成新的规则层。

不新增脚本、测试、文档生成器或兼容说明框架。三个活动文档直接改正现有段落，是覆盖当前漂移的最小完整变更。

## 2. 口径映射

| 主题 | 当前合同口径 | 目标文档位置 |
| --- | --- | --- |
| 状态前提 | 仅 `CANCELLED` 可删除 | 部署验收不变量、6.4 状态表；两份设计文档的规则/清单 |
| 可清理的任务自有历史 | 生成作业、审核记录、`DRAFT | PENDING_REVIEW | CHANGES_REQUESTED` 内容版本 | 部署验收状态表和测试数据；设计与技术说明 |
| 必须阻断的历史 | `APPROVED | SUPERSEDED` 内容、任一发布记录、非空发布修复来源 | 部署验收负向用例；设计与技术说明 |
| 权威关系 | 页面按服务端 `available_actions` 呈现，写请求仍由服务端在锁内最终校验 | 验收不变量和设计说明 |
| 删除结果 | 只清理匹配任务自有历史；其他业务历史与审计保持 | 验收预期和设计边界 |

## 3. 文件级方案

### `docs/deployed-full-functional-acceptance-plan.md`

1. 重写 `INV-11`，把“任意生产历史阻断”改为“仅任务自有未批准历史可清理、受保护历史阻断”。
2. 重写 6.4 `CANCELLED` 删除行，列出允许清理和必须阻断的边界。
3. 完善 10.2 测试对象：准备一个含生成作业/审核记录/未批准内容的可删除取消任务，并为已批准/曾批准内容、发布记录或修复来源准备至少一个受保护样例；通过正常产品/API 流程建立数据，不直接改库。

### `docs/GEO多平台内容运营系统方案设计.md`

统一已定位的业务规则、删除边界表、功能说明和测试要求。保留“不可清理下游历史”和结构化冲突原则，删除“任何生成作业或内容版本都阻断”的旧描述。

### `docs/GEO系统前后端技术与部署方案.md`

更新删除事务概述与验收清单，引用 `0033` 的任务自有历史边界；不复制锁顺序、触发器 SQL 等已由 `contracts/database.md` 权威维护的细节。

## 4. 一致性与历史处理

- 活动文档必须描述当前已实现合同。
- 归档 Trellis 任务、第二轮回归报告和 findings 保留历史原文，作为 `PS-QA2-DEC-001` 的发生与关闭证据。
- `PS-QA2-DEC-002` 的批准和实现状态只在本任务计划中注明；目标产品文档没有必要重复决策历史。

## 5. 风险、回滚与后续

- 主要风险是把“可清理未批准历史”误写成任意级联删除。通过逐项对照 `contracts/database.md:277-285,338`、OpenAPI 和删除服务守卫消除该风险。
- 文档改动可按三个目标文件逐项回退，不涉及数据、部署或运行时回滚。
- 本任务归档后另建集中回归任务；集中回归核对原缺陷、相关操作列、24 表门禁、UI/UX、对象存储与文档一致性，不并入本次文档提交。

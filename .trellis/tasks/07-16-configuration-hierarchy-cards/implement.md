# 实施计划

> 状态：已实施并通过父任务质量门。`0014` 完成类型 Prompt 到具体平台 Prompt 的替换迁移；平台投影支持“无有效规则”；任务创建和生成统一要求当前有效规则与平台 Prompt；渠道列表一次返回全部启用模型摘要。

## 0. 基线

- [x] 确认主工作目录位于 `main`，记录目标路径现有 diff，不覆盖或暂存无关改动。
- [x] 读取父任务三份规划、相关后端/前端规范及 `07-13-configuration-center-navigation` 的最终实现基线。
- [x] 运行配置契约、平台配置和 AI 渠道相关的最小基线测试，记录已有失败。

## 1. 契约与迁移

- [x] 先更新 `contracts/openapi.yaml`：平台级 Prompt 路径、可空 `active_version`、`prompt_configured`、渠道 `enabled_models` 和快照结构。
- [x] 新增 `0014` 迁移，用新表复制替换类型级 Prompt，并补齐一对多复制、孤立 Prompt 和约束测试。
- [x] 更新 ORM 与 `contracts/database.md`，删除 `platform_type_id` Prompt 所有权和旧关系。
- [x] 重新生成前端 API 类型，确认无手写兼容字段。

## 2. 后端

- [x] 重构 Prompt Schema、服务和路由为具体平台所有，移除类型级 Prompt 路径。
- [x] 平台列表支持 `active_version=null` 并返回 `prompt_configured`，消除列表投影中的逐行查询。
- [x] 在内容任务创建服务统一校验 `ACTIVE` 规则与当前 Prompt。
- [x] 在生成作业创建时按具体平台读取 Prompt，并完整写入平台身份和最终消息快照；保留旧不可变快照只读兼容。
- [x] AI 渠道列表批量加载并返回全部启用模型摘要，不暴露参数或敏感信息。
- [x] 补齐管理员权限、审计、乐观锁和错误码测试。

## 3. 前端

- [x] 把平台类型页面收敛为分类 CRUD，删除类型级 Prompt 状态和弹窗。
- [x] 调整平台管理页面，使无有效规则平台可展示、可维护并有明确状态。
- [x] 新增 Prompt 管理并列路由和页面，按平台覆盖编辑或删除普通 Markdown。
- [x] 调整内容任务平台选项，只允许同时具备有效规则和 Prompt 的平台，并显示明确不可用原因。
- [x] 调整导航、动态 loader、预取、query key 和缓存失效，避免第二套路由或 Prompt 缓存。
- [x] 重构渠道卡片层级，使用单次列表响应展示全部启用模型和空状态。

## 4. 验证

- [x] 运行迁移测试、平台配置后端目标测试、内容生成目标测试和 AI 渠道目标测试。
- [x] 运行配置中心前端目标测试、类型检查和受影响页面响应式/主题浏览器检查。
- [x] 使用浏览器网络记录确认渠道列表只有一次 `/ai-channels` 业务请求。
- [x] 运行契约检查、后端/前端测试、lint、类型检查和构建；较重集成/E2E 由父任务统一执行。
- [x] 检查 diff 不含旧 Prompt API、双读双写、默认 Prompt、固定五类或 `TYPE_BASE` 逻辑。

## 5. 完成门禁

- [x] 子任务验收标准逐条通过，并把实际变化和验证结果更新到任务记录。
- [x] 提交前给出精确原子提交范围并取得用户确认；不自动推送。
- [x] 本子任务完成后再启动 `07-16-controlled-physical-deletion`。

## 实施结果

- 契约检查、迁移测试、后端单元/集成测试、前端配置测试、类型检查、构建和关键 E2E 均通过。
- E2E 已验证 Prompt 删除后生成返回 `PLATFORM_PROMPT_MISSING`，恢复 Prompt 后可继续生成；渠道列表只发起一次 `/api/v1/ai-channels` 业务请求。
- 未保留类型级 Prompt API、双读双写、默认 Prompt 或 `TYPE_BASE` 两级生成逻辑。

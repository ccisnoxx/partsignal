# W3～W6 发布、GEO 与一致性回归执行矩阵

## 1. 执行边界与证据

| 证据编号 | 环境 | 范围 | 数据处理 |
| --- | --- | --- | --- |
| `LIVE-W3-W6-20260801` | 共享开发环境，`playwright-cli` 独立 ADMIN 会话 | 发布候选/记录/异常、GEO 观测/问题/洞察/打印、异常恢复、焦点和跨页面一致性 | 对既有不可变历史只读核对；未发送发布、修复、GEO 或删除写请求 |
| `AUTO-R2-20260731` | 本轮隔离单元、集成、E2E | 发布登记到移除、异常修复和显式解决、GEO 更正链、文件、并发、幂等和失败恢复 | `make test-unit`、`make test-integration`、`make e2e` 均零失败并完成隔离清理 |
| `DB-W3-W6-20260801` | 共享 PostgreSQL 只读查询 | 发布、异常、修复、GEO 更正链、附件和审计最终状态 | 只读查询，无数据修改 |

共享开发对象存储仍受 `PS-QA2-ENV-001` 阻断，因此文件写链采用同轮隔离 E2E 证据；共享页面只核对已经持久化的附件关系和展示。浏览器会话结束前确认没有活动请求拦截规则，随后已关闭。

## 2. W3：发布候选、登记、状态与受控删除

| 用例 | 结果 | 证据/实际结果 |
| --- | --- | --- |
| 候选、平台与账号匹配 | PASS | `/publications` 候选 Tab 返回 9 条，平台、账号、状态和操作列完整；取消确认明确说明已批准内容和审核历史保留，测试时选择“返回”且未写入 |
| 人工发布完整链 | PASS | 本轮 `frontend/tests/e2e/mvp-flow.spec.ts:264` 完成账号匹配、登记、两阶段证据、平台审核、发布、核验和移除；52 条 E2E 零失败并精确清理 |
| 发布记录列表与详情 | PASS | 记录 Tab 返回 83 条；`ada7808e-ec5f-4c5b-b746-de7ce681b7d8` 在工作台抽屉和 `/publications/:id` 均为 `REMOVED`，2 个附件、5 条状态事件、无可执行动作一致 |
| 合法/非法状态与重复发布 | PASS | 集成测试覆盖状态追加、公开历史保护、同平台内容重试、并发发布冲突和重复尝试拒绝；服务端保持最终权威 |
| 证据、失败登记与重试 | PASS | E2E 覆盖发布包失败后保留输入、重试及结果证据；单元测试覆盖 5xx 后输入与附件不丢失 |
| 受控删除 | PASS | 集成测试覆盖未公开发布记录删除、直接引用报告、共享文件保留、公开记录禁止删除，以及删除和发布并发串行化 |
| 缺失对象 | PASS | 随机 UUID 的 `/publications/:id` 返回结构化 `NOT_FOUND` 和重试入口，不伪造空详情 |

W3 核心业务合同通过，波次结论为 `PASS`。发布弹层关闭后的焦点问题归入跨页面 W6 和历史 `PS-QA-201`。

## 3. W4：发布异常、修复与显式解决

| 用例 | 结果 | 证据/实际结果 |
| --- | --- | --- |
| 异常列表与唯一 OPEN | PASS | 工作台当前 OPEN 待办为 0；API 返回 50 条已解决历史；集成测试覆盖一次发布异常只保留唯一 OPEN 待办 |
| 异常详情与历史保留 | PASS | `31f00c56-6da5-497e-8fa4-3641bddeba73` 显示 `RESOLVED`、原发布/任务、修复任务、解决意见且无可执行动作；数据库仍保留对应 OPEN 修复任务 |
| 修复任务来源与事实版本 | PASS | 同轮 E2E 创建修复任务并保留原发布上下文；集成测试覆盖修复上下文、事实候选、审核历史不可变和禁用配置阻断 |
| 显式解决独立于修复任务 | PASS | 同轮 E2E 明确调用解决动作；详情中解决时间、处理人和意见保留，修复任务没有被伪装为自动完成 |
| 已解决待办直达修复页 | FAIL | 直达 `/publication-attentions/31f00c56-6da5-497e-8fa4-3641bddeba73/repair` 仍显示完整表单和可点击“创建修复任务”，但上下文 `status=RESOLVED`、已有修复任务且 `available_actions=[]`；见 `PS-QA2-FUNC-002` |

W4 的服务端状态和历史合同通过，但修复页暴露无效操作，波次结论记为 `FAIL`。

## 4. W5：GEO 问题、观测、更正、洞察与打印

| 用例 | 结果 | 证据/实际结果 |
| --- | --- | --- |
| 观测列表、筛选与指标 | PASS | `/observations` 返回 99 条当前筛选记录，列表、指标和筛选请求均为 200；当前数据中人工观测与文章结果各 63 条 |
| 问题库 | PASS | `/observations/topics` 正常展示真实问题列表；“新增问题”弹窗可由 Escape 关闭并把焦点还给原按钮 |
| 人工观测、逐篇文章事实与证据 | PASS | 同轮 E2E 完整登记人工搜索、逐篇文章事实和截图；共享链尾详情展示文章事实、两份累计证据、备注和被更正关系 |
| 更正链与历史保留 | PASS | 链尾 `e59a852e-4db4-429d-9228-ca9287802f69` 可更正，原记录 `02873242-1024-4fe7-965b-53dc7ac89d94` 保留；同轮集成覆盖更正链、逐篇关系和附件 |
| 非链尾更正入口 | FAIL | 直达原记录 `/observations/02873242-1024-4fe7-965b-53dc7ac89d94/correct` 同时显示“当前记录不可更正”和未禁用的“追加更正记录”按钮，文件输入也仍可操作；见 `PS-QA2-FUNC-003` |
| 洞察、筛选与跨页链接 | PASS | `/observations/insights` 的趋势、平台表现、内容排行、覆盖矩阵和建议均来自 200 响应；默认日期写入 URL，内容链接可回到发布详情 |
| 打印 | PASS | `/observations/insights/print` 复用同一查询；打印媒体下导航隐藏，1280px 视口 `scrollWidth=clientWidth`，标题和数据正常，无意外横向溢出 |
| 审计 | PASS | 审计按链尾 UUID 查询得到 1 条 GEO 创建 SUCCESS 记录；更正链和文章关系仍可由详情追溯 |

W5 主流程通过，但非链尾对象仍暴露无效提交入口，波次结论记为 `FAIL`。

## 5. W6：恢复性、焦点与跨页面最终一致性

| 专项 | 结果 | 主要证据 |
| --- | --- | --- |
| 双击、Enter、重复与并发 | PASS | 同轮 E2E 覆盖键盘提交链；集成测试覆盖幂等重放、并发同键单一结果、重复 Worker 单一版本、并发发布拒绝及删除/发布串行化 |
| 迟到响应与恢复 | PASS | 生成可靠性集成测试覆盖超时后迟到响应不能获胜、丢失元数据恢复、离线积压和并发恢复锁 |
| 401 / 403 | PASS | W0 真实验证失效会话 401、ENGINEER 直达管理路由/API 403；未依赖隐藏按钮作为权限控制 |
| 409 / 422 | PASS | 同轮集成覆盖旧 revision、非法状态、文章集合变化、非链尾更正、输入校验和禁用配置，均返回结构化错误且不写入坏状态 |
| 429 | PASS | 对 `geo-insights` 注入 429 后页面显示“请求过于频繁，请稍后重试”、错误码、request ID、重试和重置；解除注入后重试成功且 URL 筛选保留 |
| 5xx | PASS | `GeoInsightsPage.test.tsx` 本轮验证 500 显式错误、重试和重置；发布单元测试验证失败后输入/证据保留 |
| 网络中断 | PASS | 浏览器把 `geo-insights` 请求中断为 `ERR_INTERNET_DISCONNECTED`，页面最终显示 `Failed to fetch` 和恢复入口；解除后重试成功且 URL 不变 |
| 刷新、后退和重登 | PASS | 发布 Tab/抽屉查询参数经刷新和后退保持；退出再登录后发布仍为 REMOVED，GEO 详情仍保留文章事实与累计证据 |
| 弹层关闭与焦点恢复 | FAIL | GEO 详情、GEO 操作菜单和问题弹窗均恢复焦点；候选 Dropdown→`modal.confirm`→“返回”落到 `BODY`，见 `PS-QA-201`；发布记录抽屉与审计详情侧栏关闭后也落到 `BODY`，见 `PS-QA2-UI-002` |
| 列表、详情、总览、审计和数据库 | PASS | 总览 OPEN 发布异常为 0，与发布工作台一致；发布 UUID 在审计中有 create/review/publish/verify/remove 共 5 条 SUCCESS；数据库最终状态见下一节 |

W6 的错误反馈、恢复和最终一致性通过，但确认框、发布抽屉和审计侧栏的焦点恢复未达标，波次结论记为 `FAIL`。

## 6. 数据库只读终态核对

| 对象 | 只读结果 |
| --- | --- |
| 发布记录 | `REMOVED`，1 条目标记录 |
| 发布事件 / 附件 | 5 条事件；2 个附件关系 |
| 发布异常 / 修复任务 | `RESOLVED` 1 条；关联修复任务 `OPEN` 1 条 |
| GEO 更正链 | `MANUAL_ARTICLE_SEARCH` 2 条；链尾后继 0 条 |
| GEO 文章结果 / 附件 | 链尾文章结果 1 条；整链附件 2 个 |
| 审计 | 目标发布 5 条；目标 GEO 链尾 1 条 |

页面、API、审计和数据库在上述最终业务状态上一致。本次共享环境浏览器验证没有执行业务写入，所有完整写链来自同轮已清理的隔离 E2E。

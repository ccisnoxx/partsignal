# 实施计划

## 0. 工作区保护与基线

- [x] 记录任务开始时 `git status`、相关文件 diff 和现有主题/性能任务改动。
- [x] 运行配置中心相关现有 E2E 或最小页面冒烟，记录当前行为。
- [x] 确认不修改 `contracts/`、`backend/`、`docs/`、部署文件和视觉基线之外的用户改动。

## 1. 路由与导航骨架

- [x] 建立 `ConfigurationLayout` 管理员守卫和五条配置子路由，`/configuration` 重定向 AI 配置。
- [x] 扩展 `routeLoaders.ts`，保持动态 import 单一来源。
- [x] 将 AppLayout 系统菜单改成树结构，支持桌面折叠、移动 Drawer、父级展开和深层选中。
- [x] 更新 `routePrefetch.ts` 及单测，只预取目标子路由，不扩大空闲预取集合。

## 2. AI 配置首页

- [x] 将渠道集合逻辑迁移到 `AIChannelsPage`。
- [x] 实现响应式渠道卡片，展示现有列表接口可提供的信息。
- [x] 保留新增、启停、删除能力；危险删除增加明确确认。
- [x] 创建后刷新列表，点击卡片进入稳定详情 URL，不查询每个渠道的模型列表。

## 3. AI 渠道详情

- [x] 将渠道详情从 Modal 迁移到 `AIChannelDetailPage`。
- [x] 拆分连接与凭据、请求 Header、模型三个可扫描区块。
- [x] 保留 Header 新增/编辑/删除、渠道更新、API Key 替换、模型发现/新增/编辑/测试/启停/删除行为。
- [x] 精简模型行操作并展示最近测试和参数摘要；敏感值保持不回显。
- [x] 为加载、空状态、错误和无效 channelId 提供明确页面反馈。

## 4. 其他配置页面

- [x] 将平台类型与 Prompt 迁移到独立页面。
- [x] 将具体平台规则及其表单从 `SettingsPage.tsx` 迁移到配置 feature。
- [x] 将审计日志迁移到独立配置页面。
- [x] 清理旧 `ConfigurationPage` Tabs 和跨 feature 导出，确保业务设置行为不变。

## 5. 样式、响应式与可访问性

- [x] 添加渠道网格、详情概览和配置区块样式，复用现有变量和主题 Token。
- [x] 验证 375、768、1024、1440 宽度以及浅色/深色主题。
- [x] 验证键盘导航、菜单展开、返回入口、卡片入口、Modal 焦点和危险操作名称。
- [x] 检查长 URL、模型 ID、Header 名和表格不会造成页面级横向溢出。

## 6. 测试与验证

- [x] 新增 AppLayout 配置子菜单与深层选中测试。
- [x] 新增配置路由、渠道卡片和渠道详情组件测试。
- [x] 更新 route loader/prefetch 测试。
- [x] 更新相关 `mvp-flow.spec.ts` 管理员配置流程，并保留普通用户不可见断言。
- [x] 添加独立配置页面视觉与 axe 场景；不覆盖无关快照。
- [x] 运行 `npm run lint`。
- [x] 运行 `npm run typecheck`。
- [x] 运行 `npm test`。
- [x] 运行 `npm run build`。
- [x] 运行配置中心相关 Playwright；资源允许时运行完整 E2E 与视觉检查。

## 7. 最终审计

- [x] 检查没有 OpenAPI/后端改动、N+1 模型请求、重复 query key 或第二套动态 import。
- [x] 检查敏感 Header/API Key 没有回显，管理员守卫覆盖所有子路由。
- [x] 检查旧 `/configuration` 可重定向，刷新渠道详情可恢复。
- [x] 检查最终 diff 未覆盖任务开始前的主题、性能、测试和截图改动。
- [x] 汇总验证结果和未运行项；未经用户确认不提交、不推送、不部署。

## 8. 获取模型与连接测试交互修订

- [x] 将远端模型结果从页面平铺区移入“获取模型”弹窗，支持筛选、已配置标记和逐个直接添加。
- [x] 保留手动添加入口，并确保远端模型添加复用现有创建接口和模型 query key。
- [x] 新增专用连接测试调用，发送唯一用户消息 `hi`，不复用正式生成的业务草稿解析。
- [x] 同步任务设计、AI 配置规范及两份 GEO 方案文档，不改变 OpenAPI、数据库和权限契约。
- [x] 运行前端配置页面单测、前端 typecheck/lint、后端 AI 边界单测和静态检查。
- [x] 扫描最终 diff，确认未覆盖现有视觉基线改动、未泄露凭据且正式生成仍执行严格四字段校验。

## 风险门槛

- 如果现有 API 无法提供某个卡片指标，删减该指标，不新增 N+1 请求或猜测字段。
- 如果拆分页面需要修改 OpenAPI 或后端权限契约，停止该部分并报告阻塞。
- 如果与正在进行的主题/冷缓存性能改动直接冲突，保留其单一 loader、Suspense 和主题边界后再继续。

# 前端测试门禁收敛实施报告

## 结论

前端单元、视觉合同和 E2E 门禁已恢复。完整前端测试连续两次零失败且分别在 300 秒内完成；完整 E2E 恢复为 `52 passed / 0 failed`。本任务只修改测试、测试规格和任务记录，没有修改产品业务代码、后端、API/数据库合同或权限状态机。

## 变更

- Prompt E2E 改用权威 `platform_prompt_id`，验证 Prompt 身份、绑定平台和预览链路。
- 同步生成作业必填 Prompt 身份、发布记录更多菜单、GEO Drawer 遮罩和移除确认文案等现行界面合同。
- GEO 页面测试使用页面级 harness，不再为七个用例重复加载完整认证与工作台壳层；7/7 继续通过，单文件约由 47.60 秒降至 35.74 秒。
- GEO E2E 使用已有“查看观测”按钮打开详情，未过滤第三方控制台告警，四类浏览器失败信号继续断言为空。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 完整前端门禁第 1 次 | Vitest 175/175、视觉合同 23/23，233.32 秒 |
| 完整前端门禁第 2 次 | Vitest 175/175、视觉合同 23/23，238.18 秒 |
| 定向 `mvp-flow` E2E | 3/3 通过，主流程 52.8 秒，总计约 1 分钟 |
| 完整 `make e2e` | 52/52 通过，4.7 分钟 |
| `npm --prefix frontend run lint` | 通过 |
| `npm --prefix frontend run typecheck` | 通过 |
| `git diff --check` | 通过 |
| E2E teardown | 临时数据库与存储均删除，开发前端恢复 |

## 边界与残余风险

- 300 秒是当前工作站的任务验收上限；稳定规范只固定权威命令、正确工作目录和证据字段，不把机器相关耗时写成全局常量。
- Vitest 仍会输出既有 CSS 解析噪声，但未过滤或吞掉告警；本次测量未证明它导致失败或挂起，因此未扩大范围处理。
- `.playwright-cli` 诊断产物属于既有未跟踪文件，已保留且不纳入本任务提交范围。

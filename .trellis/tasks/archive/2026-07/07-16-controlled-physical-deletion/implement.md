# 实施计划

> 状态：已实施并通过父任务质量门。所有物理删除由管理员接口和服务端引用预检统一裁决，冲突使用 `details.references[{type,count}]`；未增加强制删除、历史级联或归属改写。

## 0. 依赖确认

- [x] 确认 `07-16-configuration-hierarchy-cards` 已完成并通过质量门，平台 Prompt 和可空有效规则契约已是当前基线。
- [x] 确认主工作目录位于 `main`，记录目标文件现有 diff，不覆盖或暂存无关改动。
- [x] 逐项核对 ORM、迁移和 `contracts/database.md` 的直接外键，确认删除引用矩阵完整。

## 1. 契约与后端

- [x] 先更新 OpenAPI 的产品、规则版本、平台和平台账号 DELETE 路径，以及统一冲突详情 Schema。
- [x] 为每类对象实现明确的删除服务：锁行、统计完整直接引用、冲突或删除、审计。
- [x] 统一既有平台类型删除和 Prompt 删除的管理员权限、错误信封和审计行为。
- [x] 保留数据库 RESTRICT/CASCADE 的既定边界，不增加 `force`、历史级联或归属改写。
- [x] 重新生成前端类型并确认错误详情不是手写猜测类型。

## 2. 前端

- [x] 在产品页面增加仅管理员可见的物理删除入口和具体确认文案。
- [x] 在平台管理增加规则版本与具体平台删除入口；删除有效规则后刷新为“无有效规则”。
- [x] 在平台账号管理增加仅管理员可见的删除入口。
- [x] 复用平台类型和 Prompt 删除交互，统一显示结构化引用类型和数量。
- [x] 成功后精确失效产品、平台、平台类型、Prompt、账号和内容任务平台选项缓存。

## 3. 自动化验证

- [x] 后端目标测试覆盖每类删除的权限、成功、404、多引用、重试和审计。
- [x] 集成测试覆盖数据库实际外键、当前事实工作区级联和历史引用阻断。
- [x] 平台状态测试覆盖删除 `ACTIVE`、平台保留、工程师不可选、管理员新建并激活后恢复。
- [x] 前端测试覆盖按钮权限、确认文案、冲突列表和缓存刷新。
- [x] 运行契约检查、后端/前端目标测试、集成测试、lint、类型检查和构建。

## 4. 完成门禁

- [x] 检查所有删除都以服务端管理员权限和引用预检为权威，未新增隐藏级联或静默失败。
- [x] 检查错误详情列出所有真实直接引用，不依赖 message 文本或首个外键异常。
- [x] 检查删除有效规则没有自动激活替代版本，也没有新增第二个平台可用状态字段。
- [x] 提交前给出精确原子提交范围并取得用户确认；不自动推送。
- [x] 本子任务完成后再启动 `07-16-frontend-chinese-localization`。

## 实施结果

- 集成测试覆盖产品、平台规则版本、具体平台、平台账号和平台类型的引用冲突与成功删除。
- 删除未引用的 `ACTIVE` 规则版本后，平台保留且 `active_version=null`；管理员仍可配置，工程师在规则与 Prompt 条件恢复前不能创建任务。
- 按真实外键修正规划假设：发布记录直接引用平台账号，因此平台删除检查规则版本和账号，账号删除检查 `PublicationRecord`。

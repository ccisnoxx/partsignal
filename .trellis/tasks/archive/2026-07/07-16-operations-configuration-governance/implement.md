# 实施计划

> 状态：三个子任务和父任务文档更新均已实施，功能、集成和视觉质量门已通过；用户已确认原子提交范围，并授权仅更新三组受影响的 Linux 视觉基线。

## 0. 基线与工作区保护

- [x] 确认主工作目录仍在 `main`，记录 `git status` 和目标文件 diff；不得暂存或覆盖现有视觉基线等无关改动。
- [x] 以提交 `fdc1096` 后的配置中心页面为实现基线，不修改或重复执行 `07-13-configuration-center-navigation` 任务资料。
- [x] 运行最小基线：契约检查、配置中心前端测试和相关后端测试，记录已有失败。

## 1. 子任务一：配置层级与渠道卡片

- [x] 完成 `07-16-configuration-hierarchy-cards` 的契约、`0014` 迁移、后端生成读取、前端三个并列页面和渠道模型摘要。
- [x] 验证旧 Prompt 复制、孤立 Prompt 删除、平台无规则状态、无 Prompt 创建任务拒绝和作业级快照。
- [x] 验证 AI 渠道列表只有一次浏览器 API 请求，全部启用模型在卡片中完整展示。
- [x] 子任务通过质量门后按项目 Git 规则提出原子提交计划并等待确认。

## 2. 子任务二：受约束物理删除

- [x] 完成 `07-16-controlled-physical-deletion` 的产品、平台规则版本、平台、平台账号和平台类型删除边界，并统一前一子任务 Prompt 删除的权限与反馈。
- [x] 所有冲突返回稳定错误码及 `details.references[{type,count}]`，不得依赖前端猜测或首个数据库外键错误。
- [x] 验证管理员成功路径、工程师拒绝、引用冲突、清理引用后重试和审计记录。
- [x] 子任务通过质量门后按项目 Git 规则提出原子提交计划并等待确认。

## 3. 子任务三：全站中文化

- [x] 完成 `07-16-frontend-chinese-localization` 的全站用户可见文本扫描和翻译。
- [x] 保留技术术语、协议字段、业务数据和机器值，Select 只改变显示 label。
- [x] 验证浅/深色、移动端、状态标签、枚举选项和错误/空状态。
- [x] 子任务通过质量门后按项目 Git 规则提出原子提交计划并等待确认。

## 4. 父任务集成与文档

- [x] 更新 `contracts/database.md` 的 `0014` 数据所有权、删除边界和历史快照规则。
- [x] 更新两份 GEO 方案文档，移除固定五类、类型 Prompt 和 `TYPE_BASE/PLATFORM` 两级生成的冲突描述，记录动态类型、平台级 Prompt、单次完整生成和受约束删除。
- [x] 按实现结果更新 `.trellis/spec/backend/database-guidelines.md`；AI 快照契约变化时同步 `.trellis/spec/backend/ai-configuration-guidelines.md`。
- [x] 执行父任务 PRD 的逐条验收并检查三个子任务之间没有第二套权限、Prompt 读取、枚举翻译或 query key。

## 5. 验证命令

- [x] `make contract-check`
- [x] 后端目标测试：Prompt 迁移/读取、平台可用性、删除引用矩阵、渠道模型摘要。
- [x] `make test-unit`
- [x] `make test-integration`
- [x] 前端目标测试：配置路由/页面、删除冲突、状态与枚举中文标签。
- [x] 前端完整测试。
- [x] `make lint`
- [x] `make typecheck`
- [x] `make build`
- [x] E2E：两条真实业务链通过；14 条视觉用例使用 `/tmp` 隔离基线通过。
- [x] 经用户确认，仅更新 `task-*`、`configuration-ai-*`、`configuration-channel-*` 三组 Linux 基线；其余既有视觉快照保持不变。

## 6. 最终审计

- [x] 检查没有旧平台类型 Prompt API、双写表、兼容读取、固定五类或 `TYPE_BASE` 残留。
- [x] 检查所有删除均先校验引用、只允许管理员且不级联历史。
- [x] 检查旧生成作业仍可读取，新作业完整冻结具体平台和 Prompt。
- [x] 检查全站不再显示可合理翻译的英文业务文本，技术术语和值未被改写。
- [x] 检查最终 diff 只包含已识别范围；提交前再次提供精确提交计划并取得用户确认，不自动推送或部署。

## 风险与停止条件

- 若线上 `0014` 前存在无法映射但业务方要求保留的孤立 Prompt，停止迁移并重新确认；当前批准策略允许删除，不能自行增加 legacy 表。
- 若发现产品或平台存在未在设计中列出的直接外键，先补充引用矩阵和契约测试，再实现删除；不得靠捕获 `IntegrityError` 猜测。
- 若目标文件出现新的并发修改，先保留最新内容并重新核对；存在直接冲突时停止并询问用户。
- 若完整 E2E 因环境不可用无法执行，必须报告原因、已运行的替代检查和剩余风险。

## 实施与验证结果

- 已按固定顺序完成三个子任务，并更新 `contracts/database.md`、两份 GEO 方案文档和后端 Trellis 规范。
- 契约、lint、类型检查、后端单元测试 65 个、PostgreSQL 集成测试 19 个、前端 13 个文件 33 个测试和双镜像构建全部通过。
- 两条真实业务 E2E 通过，覆盖删除权限/404、平台级 Prompt、无有效规则边界、AI 生成快照、发布与 GEO 追溯。
- 14 条视觉用例使用 `/tmp` 隔离基线完整通过；经用户授权更新内容任务、AI 渠道卡片和渠道详情三组 Linux 基线，随后无更新参数复跑 3/3 通过。
- 其余视觉基线及 `.github/workflows/visual-baselines.yml`、`.trellis/config.yaml` 的既有改动保持原样，不纳入本任务提交。
- 最终扫描未发现旧类型 Prompt API、`TYPE_BASE`、固定五类或类型级 Prompt 读取残留；`PLATFORM_REVIEW` 是保留的发布状态枚举。

# 实施计划

## 1. 契约与服务端

- [x] 在 `contracts/openapi.yaml` 增加内容任务和用户 DELETE operation，扩展任务 `available_actions`，并把重置临时密码 `minLength` 改为 8。
- [x] 在内容任务服务中实现 `CANCELLED`、生成作业和内容版本引用门禁，追加成功审计。
- [x] 在身份服务中实现停用状态门禁、现有 FK 策略删除、精确外键冲突映射与成功审计。
- [x] 新增 `0027` 迁移：以事务本地用户 UUID 只放行外键触发的 `audit_logs.actor_id -> NULL`，保持其他 UPDATE/DELETE 的 `55000` 追加式门禁。
- [x] 更新路由权限和 CSRF 边界，保持内容任务删除权限与取消一致、用户删除仅管理员。
- [x] 重新生成前端 API 类型，不手改生成字段。

## 2. 前端

- [x] 在任务详情 `available_actions=DELETE` 时增加确认删除、结构化错误、成功提示和返回列表。
- [x] 在停用用户“更多操作”中增加确认删除，成功后刷新列表、汇总和当前页边界，失败保留用户。
- [x] 将重置临时密码表单最小长度改为 8，保持新建账号和自助改密 12 位。
- [x] 将平台管理列表 Logo 从 26px 调整为 24px，不修改共享组件默认值和其他消费者。

## 3. 根因回归

- [x] 后端身份集成测试：8/7 位重置边界、停用无业务引用删除、会话级联、审计 actor 置空、启用/业务引用/权限阻断。
- [x] PostgreSQL 迁移测试：合法事务目标只置空匹配 actor；无声明、目标不匹配、其他字段 UPDATE 和审计 DELETE 继续返回 `55000`；upgrade/downgrade 可执行。
- [x] 后端内容集成测试：已取消无引用删除，以及状态、生成作业、内容版本和权限阻断。
- [x] 契约测试：DELETE 路径、动作枚举、204/409 和密码边界与 Pydantic 一致。
- [x] 前端 Vitest：两个删除确认链、失败展示、刷新/导航、密码 8 位和 Logo 24px。
- [ ] E2E：账号治理流程删除精确测试账号且成功运行后无新增残留；任务删除与平台 Logo 做最小真实浏览器验证。

## 4. 权威文档

- [x] 更新 `contracts/database.md` 的用户生命周期、任务删除、外键和审计取舍。
- [x] 更新 `docs/GEO多平台内容运营系统方案设计.md`，删除“长期用户不提供物理删除”的失效表述并写入当前受约束规则。
- [x] 实施后通过 `trellis-update-spec` 更新稳定开发约束；不在规划阶段把未实现行为写成现行规范。

## 5. 验证

- [x] 运行定向后端集成/契约测试。
- [x] 运行空库升级到 Alembic head 和 `0026 ↔ 0027` 定向迁移回归。
- [x] 运行定向前端 Vitest 与 Playwright。
- [x] 运行前端 API 类型生成一致性、typecheck、lint 和 production build。
- [x] 运行后端 lint/typecheck 或项目现有等价质量入口。
- [x] 运行 `git diff --check`，检查没有新增表/字段/依赖、自动线上删除或无关改动。

## 6. 交付门禁

- [x] 独立 `trellis-check` 无未解决高/中严重级问题。
- [ ] 向用户展示精确 commit plan 并取得确认后才提交；不自动推送、部署、删除预发布账号或归档。

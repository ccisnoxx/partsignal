# 生产环境全业务流程验收执行计划

## 1. 执行前门禁

- [x] 确认当前任务处于 `in_progress`，且用户已批准最新版 `prd.md`、`design.md`、`implement.md`。
- [x] 加载 Trellis 工作流与任务文档；本任务未编辑产品代码，未触发前后端实现规范。
- [x] 只读确认 `https://geo.962850.xyz/login` 可达、部署版本包含归档与永久删除入口。
- [x] 用安全方式取得管理员登录上下文，不输出或持久化密码；确认没有同名 `prod-business-acceptance` 开放会话。
- [x] 生成本次唯一运行标识，记录开始时间；用 `playwright-cli -s=prod-business-acceptance open https://geo.962850.xyz/login` 创建会话。

## 2. 基线与只读盘点

- [x] 登录前不保留密码 DOM 证据；登录后检查工作台、导航、初始控制台和关键请求。
- [x] 记录现有知乎平台 ID/名称、当前 Prompt 可用性、可用发布账号。
- [x] 记录候选真实 AI 渠道与模型的名称、测试状态、渠道/模型启用状态；不读取敏感值。
- [x] 记录全局自然化 Prompt 是否已配置；未配置时不为本次测试创建永久单例。
- [x] 建立并完成 `report.md` 页面矩阵。

## 3. 管理页面隔离数据

- [x] 用户管理：创建唯一工程师用户与临时密码；用第二个同前缀会话完成首次改密和管理员路由权限隔离，随后关闭第二会话。
- [x] 平台类型：创建、搜索和编辑专用测试类型。
- [x] Prompt：创建并编辑专用平台 Prompt。
- [x] 平台：创建专用测试平台，绑定测试类型与 Prompt，验证筛选和引用冲突。
- [x] 业务设置：在现有知乎下创建专用测试发布账号，覆盖主要交互。
- [x] AI 渠道：检查列表、详情、请求配置脱敏和统计；按初态执行停用/启用并恢复。
- [x] 模型：真实连接测试成功，观察测试后停用，再显式启用。

## 4. 纵向业务闭环

- [x] 产品：新建唯一测试产品，保存 `PUBLIC` Markdown 事实并通过 UI 提交、批准事实版本。
- [x] 内容任务：基于批准事实和现有知乎平台创建普通删除任务；通过确认弹窗删除并验证列表不可见。
- [x] 内容任务：创建核心任务，打开详情并选择已验证启用的真实模型生成 AI 草稿。
- [x] 首次生成明确失败并记录原因；调整 Prompt 后再次生成到成功终态，检查追溯、正文和内容版本。
- [x] 全局自然化 Prompt 未配置，按计划记录条件性 `NOT_RUN`。
- [x] 内容编辑：检查生成 Markdown，提交审核并由管理员批准最终版本。
- [x] 发布管理：创建发布工作，选择知乎测试账号，完成准备；登记模拟标题、唯一测试 URL 和“站内模拟”备注。
- [x] 发布验证：提交通过且内容匹配，核对文章身份和任务 `COMPLETED`。
- [x] GEO：创建人工文章搜索观测并关联测试文章，填写模拟结果；查看详情并提交一次更正。
- [x] GEO：检查洞察、覆盖明细、打印视图和问题库。

## 5. 删除生命周期与清理

- [x] 在核心完成任务上确认普通删除入口不可用。
- [x] 归档任务，确认默认列表隐藏、归档筛选可见；恢复并核对完成状态不变。
- [x] 再次归档，打开永久删除预览；核对内部记录数量和测试 URL，确认不删除外部内容。
- [x] 输入错误确认文本验证门禁；输入固定文本 `永久删除` 执行不可逆删除。
- [x] 核对内部发布、独占 GEO 链消失，共享观测保留，审计保留最小墓碑。
- [x] 删除知乎测试发布账号、专用测试平台、Prompt、平台类型、事实版本和产品。
- [x] 停用并删除专用工程师用户。
- [x] 恢复既有 AI 渠道、模型、知乎 Prompt 和主题的初始状态；唯一残留为无删除入口的 GEO 问题主题。

## 6. 页面扫尾与报告

- [x] 逐个访问当前路由，验证数据加载和主要入口；动态路由使用本次或安全现有实体。
- [x] 检查浏览器控制台 `error`/`warning` 与关键请求；最终为 0/0，最近关键请求均 200。
- [x] 完成 `report.md`：页面矩阵、核心时间线、删除与清理、敏感信息检查、失败/未执行和残余风险。
- [x] 登出，关闭 `prod-business-acceptance` 及本任务创建的工程师会话；`playwright-cli list --all --json` 返回 `browsers: []`、`servers: []`。

## 7. 必需验证

本任务没有代码变更，不运行 lint、typecheck、build 或本地完整 E2E；它们不能证明线上真实操作。必需验证全部通过真实 UI 完成：

```bash
playwright-cli -s=prod-business-acceptance snapshot
playwright-cli -s=prod-business-acceptance console
playwright-cli -s=prod-business-acceptance requests
playwright-cli -s=prod-business-acceptance close
playwright-cli list --all --json
```

必须观察的业务断言：真实模型连接测试后停用并恢复、生成作业明确终态、任务完成态、普通删除后 404/不可见、归档/恢复投影、永久删除预览与确认门禁、永久删除后聚合不可查询、非测试配置恢复。

## 8. 可选验证

- 不执行第三方知乎页面访问或真实性验证。
- 不运行本地 `make e2e`；现有 E2E 是参考顺序，不是本次线上验收目标。
- 只有发现可重复的产品缺陷且用户另行授权修复时，才创建独立修复任务并选择定向自动化回归。

## 9. 回滚点

- 渠道/模型变更：每次状态写入后立即核对并保存初态，任何后续失败先恢复。
- 测试配置：平台 Prompt → 平台 → 平台类型按引用关系清理；知乎测试账号在发布聚合永久删除后清理。
- 核心业务：永久删除前可停止并保留唯一标识；执行永久删除后不可回滚，只能核对结果。
- 会话：任何退出路径都必须关闭当前任务命名会话，不使用 `close-all` 或 `kill-all`。

# 冻结 Response 合同权威校准实施计划

## Implementation State

本 Task 已获用户批准并进入 `in_progress`；按批准范围实施 Phase B。外部进程此前将整个工作树提交为 `e898c061` 并推进 `origin/main`，混合包含 Phase B 候选实施、task-only verifier 与无关 artifacts。本轮不自行 reset、revert、force-push 或重写历史；保留该提交事实并仅审计、修正批准范围内文件。

## Owned Files

实施允许修改：

- `contracts/openapi.yaml`
- `frontend/src/shared/api/generated/schema.d.ts`（仅由 `api:generate` 写入）
- `backend/tests/unit/test_contract.py`
- `research/final-response-authority-matrix.jsonl`（只允许根据 review 修正证据，不作产品输入）
- 本 Task planning/context/review 记录与父 Task child metadata

禁止修改 `backend/app/**`、其他 backend tests、frontend page/consumer、Makefile、CI、`contracts/database.md`、父/兄弟 Task 内容。已存在的 task-only verifier 与 validation JSON 不纳入 Task 实施或提交。

## Implementation Checklist

### 1. 实施前权威复核

- [ ] 运行 `trellis-before-dev`，重读本 Task 三件规划文档、final matrix 与 context manifests。
- [ ] 确认主工作区仍在 `main`；列出 dirty files，不回退或纳入无关改动。
- [ ] 对照 `4dd7441a` 复核三个已存候选产品 diff；确认每一处 status/schema/Header 改动都回指 final matrix 和权威 owner。
- [ ] 用 `jq`、`sort -c` 与 `awk` 检查 matrix 为 163 行、162 个 operation、身份唯一且排序稳定，`status_evidence` 与 `phase_b_statuses` 精确同键：

```bash
matrix=.trellis/tasks/09-01-frozen-contract-authority-reconciliation/research/final-response-authority-matrix.jsonl
test "$(wc -l < "$matrix" | tr -d ' ')" -eq 163
tail -n +2 "$matrix" | jq -e '
  . as $row
  | ($row.path | type == "string")
    and ($row.method | type == "string")
    and ($row.operationId | type == "string")
    and (($row.phase_b_statuses | sort) == ($row.status_evidence | keys | sort))
    and all($row.status_evidence[]; .owner_symbol != "" and .evidence != "" and .escape_or_transform != "")
' >/dev/null
tail -n +2 "$matrix" | jq -r '[.path,.method,.operationId] | @tsv' | LC_ALL=C sort -c
tail -n +2 "$matrix" | jq -r '[.path,.method,.operationId] | @tsv' | LC_ALL=C awk 'seen[$0]++ { exit 1 }'
```

- [ ] 修改根合同前完成 matrix-only critical review；无证据 status、未追踪 service 逃逸或公共 schema 矛盾都先回 planning。

### 2. 校准冻结 OpenAPI

- [ ] 按 final matrix 逐 operation 修改 response key set；所有本 Phase 4xx/5xx 复用 `#/components/responses/ErrorResponse`。
- [ ] 保留 `getAuditLog` 409；删除 `testAIModel` 502/504，并保留 `discoverAIChannelModels` 502/504 和 `testAIModel` 409。
- [ ] 将 `completeFileUpload` 精确写为 `200/401/403/404/409/422/503`。
- [ ] 为 149 个 validation-capable operation 冻结 422 ErrorResponse；确认 13 个无 validation path operation 不含 422。
- [ ] 将 `ErrorDetail.details` 加入 required；保留 object/default `{}` 语义。
- [ ] 修正 health `checks` nullable、GEO basis discriminator 和四个 generation/humanization snapshot `anyOf` 位置。
- [ ] 核对两个 CSV 的 `text/csv`、string body，设置 `Content-Disposition.required: true`；不新增 `Content-Type` Header。
- [ ] 确认 diff 不含 `X-Request-ID` request/400/response metadata、`Set-Cookie`、500/default 或 runtime metadata helper。

### 3. 同步 generated client 与静态测试

- [ ] 运行 `npm --prefix frontend run api:generate`；不手工编辑 generated file。
- [ ] 更新 `backend/tests/unit/test_contract.py` 中受影响的已有精确 status set，不放宽断言。
- [ ] 新增/收敛 ErrorEnvelope handler sentinel、`testAIModel` provider 502/504 → 200 `FAILED` sentinel、revision race 409 反例、`completeFileUpload` 503 sentinel 和两个 CSV route sentinel。
- [ ] 新增公共 schema/media/Header 断言；不读 matrix，不复制 comparator。
- [ ] 审查 generated diff 只含预期公共影响；如需修改 frontend consumer，停止并回父 Task 拆分。

### 4. Review Gates

- [ ] `trellis-check` 核对 spec、required validation、实际 diff 和边界。
- [ ] `critical_reviewer` 独立抽取 root contract diff 中所有新增/删除 status，逐项回指 route/dependency/service/HTTP 证据；独立核对 149/13 validation 分界、特殊 5xx、公共 schema、CSV Header、Phase X 边界和 generated 来源。
- [ ] reviewer 必须明确证明：没有猜测 status，没有删除真实 response，没有改变业务语义，没有修改 `backend/app/**`，没有提前加入 `X-Request-ID`/400。
- [ ] 独立 review 只允许一次完整 review 和一次受影响路径复核；如仍有 MEDIUM 及以上问题，报告并停止。
- [ ] 提交前展示精确 commit plan 并得到用户确认；不纳入无关 dirty files/artifacts，不自动 push。

## Required Validation

```bash
npm --prefix frontend run api:generate
npm --prefix frontend run api:check
npm --prefix frontend run typecheck
UV_CACHE_DIR=.cache/uv uv run --project backend pytest backend/tests/unit/test_contract.py
UV_CACHE_DIR=.cache/uv uv run --project backend pytest backend/tests/unit/test_contract_check.py
UV_CACHE_DIR=.cache/uv uv run --project backend python -m app.tools.contract_check contracts/openapi.yaml
UV_CACHE_DIR=.cache/uv uv run --project backend ruff check backend/tests/unit/test_contract.py
python3 ./.trellis/scripts/task.py validate .trellis/tasks/09-01-frozen-contract-authority-reconciliation
```

`typecheck` 是 required：`api:check` 只证明 generated bytes 与 OpenAPI 同步，不证明现有 consumer 可编译。

另单独执行只读中间态诊断，确认退出码恰为 1：

```bash
set +e
UV_CACHE_DIR=.cache/uv uv run --project backend python -m app.tools.contract_check \
  --response-report contracts/openapi.yaml
phase_b_response_report_rc=$?
set -e
test "$phase_b_response_report_rc" -eq 1
```

不保存或读取 drift baseline，不使用 filter/suppress/allowlist。退出 2 必须阻断；退出 0 也与本 Phase 边界矛盾，必须调查。

## Optional Validation

```bash
make contract-check
UV_CACHE_DIR=.cache/uv uv run --project backend pytest backend/tests/unit
npm --prefix frontend run test
```

`make contract-check` 是 required 子门禁的包装复核，本 Phase 不改 Makefile，故列为 optional 避免重复成本。不运行 frontend build/E2E、backend integration、数据库、provider、对象存储或生产调用。

## Documentation Check

- [ ] OpenAPI、generated client、静态测试和 final matrix 对同一 status/schema/Header 事实一致，且 OpenAPI 仍是唯一可编辑产品合同。
- [ ] 不更新 `contracts/database.md`：本 Phase 没有数据模型、约束或持久化行为变化。
- [ ] 不更新 `.trellis/spec/`：本 Phase 执行已有错误处理、跨层合同和 CI 约束。
- [ ] 新增 sentinel 的必要注释、docstring 和开发者可见文本使用中文；不为机械断言添加重复注释。

## Rollback

- Phase C 开始前，同批恢复 OpenAPI、generated client 和静态测试；matrix/review 证据保留并标记 rejected/superseded。
- Phase C 开始后优先 roll-forward；必须回退时按 `F → X → E → D → C → B` 逆序恢复。
- 禁止以忽略 status、基线、overlay 或修改 Phase A comparator 恢复绿灯。

## Implementation Gate

- [x] 用户已审阅并明确批准本版 Phase B 规划，Task 已进入 `in_progress`。
- [x] 外部提交 `e898c061` 的混合变更事实已保留；本 Task 不自行改写历史。
- [ ] `implement.jsonl` / `check.jsonl` 已包含真实 spec/research context，`task.py validate` 通过。
- [ ] 父 Task 和 `frontend-v2-functional-contract-conformance-baseline` 保持 `planning`；`v2-live-readonly-acceptance` 保持 `in_progress`，三者均不归档。

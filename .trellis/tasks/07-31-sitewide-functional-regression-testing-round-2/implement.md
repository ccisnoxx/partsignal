# 全项目功能回归测试（第二轮）：执行计划

## 0. 开始门禁

- [x] 用户评审并明确批准 `prd.md`、`design.md`、`implement.md`。
- [x] 获得批准后运行 `python3 ./.trellis/scripts/task.py start 07-31-sitewide-functional-regression-testing-round-2`；批准前不启动测试写操作。
- [x] 运行 `trellis-before-dev`，重新读取本任务文档、相关 `.trellis/spec/` 和权威合同。
- [x] 确认主工作区为 `main`，记录 Git 状态；保留并排除用户已有的 `.playwright-cli/` 等未跟踪诊断产物。
- [x] 建立 `run-id`、`E2E-FULL-<run-id>` 数据前缀和证据目录。

## 1. 冻结本轮清单

- [x] 记录提交、迁移 head、Compose 服务/镜像、Node/Python、浏览器版本和北京时间。
- [x] 从 `App.tsx` 生成当前路由、动态路由、角色和主操作清单。
- [x] 从 OpenAPI、数据库合同和 Router 刷新接口、状态机与 13 个 DELETE 清单。
- [x] 从当前页面和跨页测试刷新 24 表、58 个长文本槽和全部操作列。
- [x] 创建 `research/coverage-matrix.md` 与 `research/table-action-delete-matrix.md`，全部初始为 `NOT_RUN`。

## 2. 开发环境门禁

- [x] 验证 API、数据库、Redis/Worker 和前端健康。共享开发 `fake-oss` 另因在线依赖下载/DNS 失败退出，已登记 `PS-QA2-ENV-001`，不影响本条列出的服务。
- [x] 通过前端同源 `/api` 验证健康响应、匿名会话探测和真实管理员登录。
- [x] 重启前端容器后重复代理与登录检查，并确认新日志无 `ECONNREFUSED`。
- [x] 用一个包含真实登录准备流程的隔离 Playwright 用例确认 E2E 不受开发代理配置影响，并检查精确清理。
- [x] 门禁失败则记录请求、响应、日志和受影响范围；不在本任务修复。

## 3. 必需自动化基线

逐条保留命令、通过/失败/跳过数、耗时和失败归因：

```bash
make contract-check
make test-unit
make test-integration
make e2e
make lint
make typecheck
make build
```

- [x] 合同与生成类型一致性完成：`make contract-check` 通过，耗时 1.42 秒。
- [x] 后端单元、前端 Vitest 与视觉合同完成：140 + 175 + 23 例均通过，耗时 236.93 秒。
- [x] PostgreSQL 集成测试完成：68 例均通过，耗时 88.80 秒。
- [x] 隔离 E2E 完成：52 例均通过，耗时 289.24 秒；数据库与临时存储完成精确清理。
- [x] lint、类型检查和构建完成：分别耗时 3.95、3.38、48.47 秒，均通过。
- [x] 七项命令均为零失败，无失败归因项；全程未修改产品代码或测试。

可选聚合检查：

```bash
make verify
```

仅当它能提供前述命令未覆盖的新证据时运行；否则在报告注明重复而跳过。

## 4. W0～W2：配置、产品、事实与内容

- [x] W0：未登录/错误登录/正确登录/刷新/退出/过期/后退；管理员与工程师导航和直达权限。
- [x] W0：用户创建、编辑、停用、批量操作、最后管理员、删除和审计。
- [x] W0：平台类型、平台、Prompt/revision/绑定、发布账号、AI 渠道/Header/模型全流程。
- [x] W1：产品和 PUBLIC/RESTRICTED 事实的创建、提交、退回、修订、批准、退役和不可变检查。
- [x] W2：内容任务、AI/人工首稿、自然化、生成失败、审核、修订、批准、快照和历史。
- [x] 对非法状态、旧 revision、未知事实、禁用配置、重复提交、迟到结果和失败恢复逐项记录。

执行明细见 `research/w0-w2-matrix.md`。W0 因 `PS-QA2-UI-001` 记为 FAIL，W1 为 PASS，W2 因 `PS-QA2-FUNC-001` 记为 FAIL；本任务仅测试和报告，未修改产品代码或既有测试。

## 5. W3～W6：发布、GEO 与一致性

- [x] W3：候选、账号匹配、发布登记、合法/非法状态、证据、核验和受控删除。
- [x] W4：发布异常、唯一 OPEN 待办、修复任务、显式解决和历史保留。
- [x] W5：问题库、人工观测、文章事实、更正链、洞察、筛选、打印和审计。
- [x] W6：双击、Enter、刷新、后退、关闭弹窗、两个标签页 revision/状态冲突。
- [x] W6：401、403、409、422、429、5xx 和网络中断时的反馈、输入保留与恢复。
- [x] 对列表、详情、总览、审计、重登和数据库最终状态做一致性核对。

执行结果见 `research/w3-w6-matrix.md`：W3 `PASS`；W4 因 `PS-QA2-FUNC-002`、W5 因 `PS-QA2-FUNC-003`、W6 因历史 `PS-QA-201` 与新增 `PS-QA2-UI-002` 而记为 `FAIL`。共享环境未执行业务写入，完整写链使用本轮已精确清理的隔离 E2E 证据。

## 6. 24 表与全部操作列专项

- [x] 逐表执行主数据、查询、状态、详情、返回、空、失败、权限和总数口径。
- [x] 逐项盘点当前操作列；每个动作至少执行一次正向分支，状态相关动作覆盖实际分支。
- [x] 验证禁止状态、直接请求、确认期间状态变化、失败恢复、query invalidation 和跨页面结果。
- [x] 逐项完成鼠标与键盘打开、确认、取消、重复点击和焦点恢复。
- [x] 运行现有长文本/几何回归并用真实浏览器验证 58 个文本槽、固定列、内部滚动和 Tooltip。
- [x] 同一共享根因合并为一个缺陷并列出全部受影响页面。

执行结果见 `research/table-action-delete-matrix.md`：24/24 张表及 58 个长文本槽完成桌面、窄屏、固定列、局部滚动和键盘核验，几何专项为 `PASS`；合并操作逻辑、确认/抽屉焦点、删除术语及影响说明后，8 表 `PASS`、16 表 `FAIL`，新增 `PS-QA2-UI-002`、`PS-QA2-TEST-001`，并确认 `PS-QA-201`～`PS-QA-203` 仍存在。破坏性 DELETE 的逐接口副作用不在共享环境重复执行，仍由第 7 节在隔离数据库中专项验证。

## 7. 13 个 DELETE 接口专项

对用户、产品、事实版本、内容任务、发布账号、发布记录、GEO 人工观测链、平台类型、平台 Prompt、平台、AI 渠道、AI Header、AI 模型逐项执行：

- [x] 未登录、工程师/管理员及适用角色、CSRF、目标不存在、旧 revision。
- [x] 最小合法删除、状态禁止、关联占用和受保护历史。
- [x] 直接重复 DELETE，以及同目标双请求或确认打开后服务端状态变化。
- [x] UI 对象名称、影响说明、取消、加载、防重复、成功和结构化失败。
- [x] 数据库原子性、关联关系、附件清理调度、审计数量/内容和敏感信息。
- [x] 列表总数、详情、总览、缓存、刷新和重登后的结果一致性。
- [x] 无法适用的分支用 `NOT_APPLICABLE` 加合同依据，不留无解释 `NOT_RUN`。

优先复用第一轮 `delete-boundary-probe.py`、`delete-success-repeat-probe.py` 和现有集成测试；只有缺少并发或 UI 证据时才增加最小任务证据探针，不修改产品代码或现有测试。

执行结果见 `research/table-action-delete-matrix.md`：权限、CSRF、不存在、合法删除、顺序重复、状态/引用阻断与数据库副作用均为 13/13 `PASS`；同目标并发和审计为 11/13 `PASS`，AI 渠道与 Header 因 `PS-QA2-DELETE-001` 记为 `FAIL`。结合既有 `PS-QA-201`～`203`，13 个入口综合为 1 `PASS`、12 `FAIL`。定向 PostgreSQL 集成测试 10/10、前端单测 97/97、E2E 5/5 通过；本轮隔离数据库和临时存储已精确清理，开发服务恢复健康。

## 8. 全路由 UI/UX

- [x] 全部当前路由在 `1440×1000` 和 `375×900` 完成功能状态、console、requests 与布局检查。
- [x] 代表页面在 `1024×768`、`320×800` 验证布局临界点。
- [x] 登录、配置、数据列表、编辑审核、发布/GEO 五类页面执行真实 200% 缩放。
- [x] 浅色、深色、跟随系统检查文字、边界、状态、Tooltip、表格、图表和焦点。
- [x] 仅键盘完成登录、导航、操作菜单、关键表单、确认/取消并验证焦点恢复。
- [x] 检查 reduced-motion、文字 4.5:1、非文字界面 3:1 和非颜色单一表达。
- [x] 截图只作视觉证据；功能结果同时记录页面状态、请求或数据副作用。

执行结果见 `research/ui-ux-matrix.md`：26 个功能路径及通配入口完成 54 次主视口扫描，布局、可访问标题、移动触控和 API 响应均通过；临界断点、五类页面真实 200% 缩放、三主题、reduced-motion、对比度与非颜色表达通过。仅键盘/焦点因 `PS-QA-201`、`PS-QA2-UI-002` 为 `FAIL`；新增 `PS-QA2-UI-003`，GEO 历史证据图请求则归入既有 `PS-QA2-ENV-001`。逐路由综合为 9 `PASS`、17 `FAIL`、0 `NOT_RUN`，本节未修改产品代码或现有测试。

## 9. 历史缺陷回归

- [x] 复测 `PS-QA-001`～`PS-QA-004` 原步骤。
- [x] 复测 `PS-QA-101`～`PS-QA-110` 对应门禁和数据隔离结果。
- [x] 复测 24 表长文本/缩放、内容任务术语、发布操作和开发代理。
- [x] 优先确认 `PS-QA-201`～`PS-QA-203` 的当前状态。
- [x] 补齐第一轮删除并发、确认期间状态变化、迟到响应/缓存和全部操作列键盘链。

执行结果见 `research/history-regression-matrix.md`：`PS-QA-001`～`004` 与 `PS-QA-101`～`110` 共 14 项按原步骤全部 `PASS`；为满足 `PS-QA-110`，本节补跑第二次完整前端门禁，Vitest 175/175、视觉合同 23/23，`real 241.37s`，与本轮第一次结果共同满足连续两次零失败且各不超过 300 秒。24 表/58 个长文本槽/真实缩放、内容任务术语、发布操作和开发代理均未回归；`PS-QA-201`～`203` 仍为 `FAIL`。第一轮删除并发、确认期间状态变化、迟到响应/缓存与操作列键盘链均已覆盖，实际发现继续归入 `PS-QA2-DELETE-001`、`PS-QA-201` 和 `PS-QA2-UI-002`。本节未修改产品代码或现有测试。

## 10. 缺陷与报告

- [x] 创建 `research/findings.md`：ID、类型、P0～P3、角色、前置对象、最短步骤、期望/实际、错误码、证据和影响范围。
- [x] 产品/合同/文档歧义单列决策项，不伪装成实现缺陷。
- [x] 每个 P0/P1 至少用合同、源码调用链、真实请求、数据库或审计证据之一复核。
- [x] 创建 `report.md`：环境、覆盖、门禁、W0～W6、表格/操作/删除、UI/UX、历史回归、缺陷、阻断和残余风险。
- [x] 矩阵、findings 与报告状态一致；未执行不计通过，核心 `BLOCKED` 不给出全项目通过结论。
- [x] 给出后续修复任务建议和最小权威修复位置，但不创建或实施修复任务。

执行结果见 `report.md`：七项必需自动化门禁全部通过，但全项目验收结论为 `FAIL`。本轮共确认 12 个问题，其中产品功能/UI 10 个、测试门禁 1 个、开发环境 1 个；P2×9、P3×3，P0/P1 为 0。`PS-QA2-DEC-001`、`PS-QA2-DEC-002` 作为产品/文档决策项单列，不计入缺陷统计。报告已汇总环境、覆盖、W0～W6、24 表/操作列、13 DELETE、全路由 UI/UX、历史回归、阻断、残余风险和 8 组后续任务建议；本节未创建修复任务，也未修改产品代码或现有测试。第 11 节仍需执行最终清理与退出门禁。

## 11. 清理与退出门禁

- [x] 关闭本轮命名浏览器会话，删除未跟踪认证状态。
- [x] 确认隔离数据库、临时存储和进程均已清理；共享开发库不做广泛清扫。
- [x] 清理本轮允许删除的对象；按合同必须保留的历史对象登记 ID 和原因。
- [x] 运行 `git diff --check` 与 `git status --short`。
- [x] 确认差异仅为任务规划、测试矩阵、证据索引和报告，无产品代码、现有测试、合同或权威文档改动。
- [x] 向用户提交最终报告与建议；未获单独批准不提交、不归档、不推送。

最终退出核对见 `report.md` 第 10、12 节：本轮三个隔离数据库、临时存储、认证状态和 E2E 进程均为 0；W0/W1 自建业务对象均不存在，相关 9 条身份审计与 15 条产品/事实/任务审计按追加历史合同保留。当前两个零连接旧测试库和三个早于本任务冻结时间创建的 Playwright 会话不属于本轮，均未删除或关闭。开发 API、数据库、Redis、Worker、Scheduler、前端与同源 `/api` 健康，`fake-oss` 仍为已报告的 `PS-QA2-ENV-001`。最终差异仅为任务资料与两份证据探针，另有用户既有 Playwright 诊断产物；未提交、归档或推送。

## 12. 验证分类

### 必需验证

- 开发环境同源代理、真实登录和重启后可用性。
- `make contract-check`、`make test-unit`、`make test-integration`、`make e2e`、`make lint`、`make typecheck`、`make build`。
- W0～W6、当前全部路由、24 表/全部操作列、13 DELETE、UI/UX 和历史缺陷回归。
- 隔离资源清理、矩阵一致性和任务差异检查。

这些检查可以得到 `FAIL` 或 `BLOCKED`，但必须完成归因和报告；测试任务的完成条件是覆盖与报告真实完整，不是强行得到全绿结果。

### 可选验证

- `make verify`：仅在提供新增覆盖时运行。
- 真实第三方 AI smoke：仅在已有专用测试凭据且不会产生外部副作用时运行，否则 `NOT_APPLICABLE`。
- 生产部署页面：本任务不执行写入或破坏性验证；需要时另行立项。

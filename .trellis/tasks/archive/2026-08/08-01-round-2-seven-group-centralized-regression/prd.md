# 第二轮七组修复集中回归

## 1. 目标

在当前 `main` 上对第二轮全项目回归的修复闭环做一次集中验证，重新取得独立证据，确认原报告中的 12 个问题、`PS-QA2-DEC-001` 文档漂移以及已批准实施的 `PS-QA2-DEC-002` 全局 `available_actions` 合同均已按当前实现收敛。

本任务只执行回归、记录证据并输出结论，不修改产品代码、现有测试、合同、迁移、部署配置或活动产品文档。发现的新问题按共同根因另建独立 Trellis 修复任务，不在本任务中顺手修复。

## 2. 已确认背景

- 权威缺陷基线为 `.trellis/tasks/archive/2026-08/07-31-sitewide-functional-regression-testing-round-2/report.md` 与 `research/findings.md`；原冻结提交为 `84f9dc222ff01a68ff38e6407d96acb65e926eeb`。
- 原报告登记 10 个产品功能/UI 问题、1 个测试门禁问题、1 个共享开发环境问题，并单列 `PS-QA2-DEC-001`、`PS-QA2-DEC-002` 两项决策。
- `PS-QA2-DELETE-001` 已由先行任务修复，工作提交为 `5808545`；剩余七组工作提交依次为：共享开发对象存储 `62c4e16`、全局动作投影 `2e59943`、浮层焦点 `95b34bc`、危险删除说明 `6c45c09`、前端资源兼容 `f491809`、24 表门禁 `6f8ca05`、验收文档同步 `fab7e9c`。
- 上述八个修复任务均已归档。`PS-QA2-DEC-002` 已经用户批准并由 `2e59943` 实施，不再作为待决策项；本任务只验证合同和消费者没有回退。
- 当前 Compose 的开发 `fake-oss` 配置已使用 `python -m app.files.fake_server`。主机 `127.0.0.1:19001` 当前仍由范围外 staging `fake-oss` 占用；集中回归不得停止或改写该服务，应使用不落盘的临时端口覆盖验证开发服务。
- 当前工作树只有既有 `.playwright-cli/` 与 `frontend/.playwright-cli/` 未跟踪诊断产物；必须保留、排除且不纳入任务差异。

## 3. 范围内需求

### R1. 冻结集中回归基线

- 在实施开始时记录当前提交、分支、Alembic head、Node/Python/Playwright 版本、Compose 服务状态、北京时间和唯一 `run-id`。
- 创建 `research/regression-matrix.md`，将全部待验证项初始化为 `NOT_RUN`；旧任务结果只用于定义步骤，不直接继承为 `PASS`。
- 判定只使用 `PASS`、`FAIL`、`BLOCKED`、`NOT_APPLICABLE`；不得留下无解释的核心 `NOT_RUN`。

### R2. 自动化与合同门禁

- 重新执行合同、单元/组件、PostgreSQL 集成、隔离 E2E、lint、类型检查和构建门禁。
- 记录每条命令的结果、数量、耗时和失败归因；隔离 E2E 必须确认数据库与临时对象存储精确清理。
- 失败不自动授权修复；仅记录、定位并继续不依赖该失败的验证。

### R3. 原问题逐项关闭

集中回归矩阵必须覆盖：

1. `PS-QA2-FUNC-001`～`003`：终态作业重试、已解决发布异常修复页、非链尾 GEO 更正页均只消费服务端动作投影，且无无效写入口。
2. `PS-QA2-DELETE-001`：AI 渠道与 Header 同目标并发删除均为单一 `204`、落后请求 `404`、单一成功审计和单次失效副作用。
3. `PS-QA-201`、`PS-QA2-UI-002`：Dropdown→确认、发布详情抽屉、桌面审计侧栏和移动审计 Drawer 关闭后，焦点回到原触发器。
4. `PS-QA-202`、`PS-QA-203`：六类危险删除确认使用业务语言，AI Header 明确说明渠道和模型测试状态失效，不出现用户可见“物理删除”。
5. `PS-QA2-UI-001`、`PS-QA2-UI-003`：登录入口 favicon 请求成功且不再请求 `/favicon.ico`；发布详情 Timeline 无 `items.children` 弃用警告。
6. `PS-QA2-TEST-001`：24 项均以唯一 `role=region`/`aria-label` 为目标，在 `1440×1000` 与 `375×900` 完成门禁；两张弹窗表限定在各自 dialog，背景表不能替代目标。
7. `PS-QA2-ENV-001`：开发 `fake-oss` 不触发运行期依赖同步，在不停止范围外 staging 的条件下完成真实 upload intent、PUT、HEAD/complete、GET/download 与精确清理。

### R4. 全局动作投影与相关操作列

- 复核已批准的 `PS-QA2-DEC-002`：现有资源绑定的状态、危险、编辑、保存、凭据及子资源命令继续由服务端 `available_actions` 投影；前端不恢复基于角色或局部状态的同类资格旁路。
- 覆盖用户、产品/事实、平台/Prompt、AI 渠道/Header/模型、发布账号、内容任务/作业/版本、发布候选/记录/异常和 GEO 更正/删除等既有命令面。
- 集合级创建、纯导航/查看/复制/筛选/导出/打印、认证自服务和文件传输内部动作继续按已批准边界排除，不扩大合同。

### R5. 文档与合同一致性

- 复核三份活动文档已使用 `0033_task_owned_history_delete` 的当前受控删除口径：仅 `CANCELLED` 内容任务可进入删除判定；允许清理任务拥有的生成作业、审核记录和未批准内容版本；批准/被取代内容、发布记录或修复来源继续阻断。
- 复核 `contracts/openapi.yaml`、`contracts/database.md`、实现、测试、`.trellis/spec/backend/available-actions-contract.md` 与活动文档没有互相矛盾。
- 原第二轮报告和已归档任务作为历史证据保持不修改。

### R6. 结果与缺陷分流

- 输出 `research/regression-matrix.md` 和 `report.md`，逐项关联缺陷 ID、修复提交、验证方式、结果与证据。
- 只有全部核心项无 `FAIL/BLOCKED/NOT_RUN` 时，报告才能给出“第二轮修复闭环通过”；否则明确影响和最小权威修复位置。
- 新问题不得在本任务内修改代码；按根因建议独立后续任务，避免重新形成跨前端、后端、测试和基础设施的巨大修复任务。

## 4. 范围外

- 不再执行一整轮全路由 W0～W6 功能验收，也不重做全部 13 个 DELETE 接口矩阵；本任务只覆盖原缺陷、相关动作列、24 表门禁及必要的共享边界。
- 不修改后端、前端、合同、数据库迁移、现有测试、依赖、Compose、活动产品文档或 `.trellis/spec/`。
- 不创建新的通用测试框架、页面对象、兼容层或长期探针；优先复用现有测试、脚本和已归档证据步骤。
- 不停止或修改范围外 staging 服务，不清扫共享数据库、共享对象存储卷或既有测试数据库。
- 不调用真实第三方 AI，不执行生产写入、发布、压力、容量或渗透测试。
- 不删除、移动或提交 `.playwright-cli/`、`frontend/.playwright-cli/` 诊断产物；不自动推送。

## 5. 验收标准

- [x] AC1：冻结当前提交、迁移、版本、服务状态和 `run-id`，集中回归矩阵全部从 `NOT_RUN` 开始。
- [x] AC2：七项自动化门禁均取得本轮结果、数量、耗时和清理证据；任何失败均有归因。
- [x] AC3：原报告 12 个问题逐项取得独立关闭结果，`PS-QA2-DEC-001/002` 分别取得文档和合同一致性结果。
- [x] AC4：全局资源命令面未恢复前端资格旁路，三个原功能缺陷在组件、服务和真实浏览器边界均无效动作误导。
- [ ] AC5：Dropdown→确认、发布抽屉、桌面审计侧栏和移动审计 Drawer 的关闭链均恢复原触发器焦点。
- [x] AC6：六类危险删除确认内容与服务端真实副作用一致，且目标源码和页面中无用户可见“物理删除”。
- [ ] AC7：favicon、Timeline 兼容与 console/network 基线通过；不以静态文件存在替代浏览器请求证据。
- [x] AC8：24/24 项在两个主视口命中唯一目标；两张弹窗表限定在 dialog，目标缺失的断言语义明确失败。
- [x] AC9：共享开发对象存储在安全临时端口上持续运行，真实文件链和精确清理通过，日志无 `uv run`、PyPI 或依赖同步。
- [x] AC10：三份活动文档、API/数据库合同、当前实现、测试和稳定 spec 对内容任务删除及动作投影的描述一致。
- [x] AC11：`research/regression-matrix.md` 与 `report.md` 状态一致，所有失败/阻断和残余风险可追溯，无秘密或认证状态。
- [x] AC12：任务创建的数据、对象、临时端口覆盖、命名浏览器会话和隔离资源均已精确清理；Git 差异仅包含本任务资料与报告，既有诊断产物保持未跟踪。

## 6. 已决策项与未决问题

- `PS-QA2-DEC-002` 已批准实施，本任务不得重新把它降级为待选方案，也不得扩大已批准动作范围。
- 集中回归采用“测试与报告任务”边界；发现缺陷后拆分，不在本任务修复。
- 当前没有阻塞规划的产品、范围、兼容或风险决策。

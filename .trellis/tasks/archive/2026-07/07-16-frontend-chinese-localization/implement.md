# 实施计划

> 状态：已实施并通过父任务功能质量门。用户可见业务文本、状态和稳定枚举已中文化，技术标识符与机器值保持不变；未引入 i18n 依赖或第二套业务状态。

## 0. 基线与清单

- [x] 确认前两个子任务已完成并通过质量门，最终路由和页面结构稳定。
- [x] 确认主工作目录位于 `main`，记录前端和直接展示错误的后端文件现有 diff。
- [x] 枚举全部可访问路由和角色路径，用 `rg` 建立英文候选清单并标记保留术语。

## 1. 共享显示文本

- [x] 先补全跨页面复用的状态、角色和稳定枚举中文映射，删除同义重复映射。
- [x] 确保所有 `{value,label}` 只改变显示 label，不改机器 value。
- [x] 把全局错误兜底、通知和通用空状态改为准确中文。

## 2. 逐业务域中文化

- [x] 登录、首次改密、布局、概览和用户管理。
- [x] 产品事实、事实版本、审核和证据相关页面。
- [x] 内容任务、生成作业、内容版本、审核和发布页面。
- [x] GEO 观测、文件和分析页面。
- [x] AI 配置、平台类型、平台管理、Prompt 管理、审计和设置页面。
- [x] 对直接展示的英文后端业务 message，在权威异常出处改中文并保持错误码不变。

## 3. 测试与视觉检查

- [x] 更新受影响测试文案断言，优先保留稳定角色和结构定位。
- [x] 测试状态/枚举显示与提交值分离，确认请求载荷没有变化。
- [x] 运行前端测试、lint、类型检查和构建。
- [x] 用管理员和工程师账号检查关键页面的正常、空、校验、错误和删除冲突状态。
- [x] 检查 375/768/1024/1440 宽度及浅/深色主题，无新增横向滚动、遮挡或不可读截断。

## 4. 完成门禁

- [x] 复跑英文候选扫描，逐项记录保留理由，确认没有可合理翻译的用户可见英文。
- [x] 检查未改变 API 字段、枚举值、模型/参数/Header/URL 原值、权限或业务流程。
- [x] 检查没有引入 i18n 依赖、全局无边界字典或与业务无关的样式重构。
- [x] 提交前给出精确原子提交范围并取得用户确认；不自动推送。
- [x] 子任务完成后返回父任务执行文档更新和跨任务 E2E 验收。

## 实施结果

- 前端 12 个测试文件共 30 个测试、ESLint、TypeScript、生产构建及关键业务 E2E 通过。
- `enumLabels` 和 `StatusTag` 统一稳定枚举与状态显示；Select 仅改变 `label`，提交的 API 枚举值未变。
- 14 条视觉用例已使用 `/tmp` 隔离基线完整通过，覆盖四档宽度、双主题与 Axe；工作区旧基线的差异来自预期中文化及渠道卡片结构变化，本次未覆盖或更新任何现有视觉基线文件。

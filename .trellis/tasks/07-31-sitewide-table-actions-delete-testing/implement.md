# 全项目功能、业务逻辑与 UI/UX 重新验收：执行计划

## 本轮执行记录（2026-07-31）

- 前序“验收门禁恢复”和“产品 UI/UX 缺陷修复”任务完成后，本任务已恢复执行。
- 当前集成结果为 `68 passed`；E2E 为 `51 passed / 1 failed`，唯一失败已归因为可复用 Prompt 页面旧文案断言。
- 收尾 `make test-unit` 中后端再次 `140 passed`；前端全量 Vitest 补跑异常挂起，已按不重复失败检查的规则登记为 PS-QA-110。
- 24 张业务表的桌面/移动运行边界和五类表格真实 200% 缩放通过。
- 两个隔离探针完成 13 个 DELETE 的权限、CSRF、成功、重复、数据库消失与成功审计矩阵，均为 13/13 通过。
- 当前确认 3 个 P2 产品 UI/UX 缺陷和 1 个 P2 测试漂移；完整结果已同步到三份 research 矩阵和 `report.md`。
- 本轮未修改业务代码、现有测试、合同或权威设计文档；清单中的未覆盖项按 `PASS/FAIL/BLOCKED/NOT_RUN/NOT_APPLICABLE` 记录，不折算通过。

## 0. 开始门禁

- [ ] 用户评审并明确批准最新 `prd.md`、`design.md`、`implement.md`。
- [ ] 批准后运行 `task.py start`，进入执行阶段；规划批准前不启动测试写操作。
- [ ] 运行 `trellis-before-dev`，重新读取本任务三份文档、相关前后端规范和权威合同。
- [ ] 确认主工作区位于 `main`，记录提交、迁移 head、目标环境与已有未提交文件；不创建分支、不提交、不推送。
- [ ] 建立本轮 `run-id`、证据目录和测试对象命名前缀 `E2E-FULL-<run-id>`。

## 1. 建立覆盖清单

- [ ] 从 `frontend/src/app/App.tsx` 建立全部路由、页面、角色和主要任务清单。
- [ ] 从 OpenAPI、Router 和现有 E2E 建立业务接口、状态机和跨域链路清单。
- [ ] 将 `docs/deployed-full-functional-acceptance-plan.md` 的风险 R01–R13、INV-01–INV-17、W0–W6 与当前合同逐项核对；旧内容任务删除规则按 `contracts/database.md:279` 更正为本轮判定依据。
- [ ] 创建 `research/coverage-matrix.md`，所有用例初始为 `NOT_RUN`，不得预填通过。
- [ ] 创建 `research/table-action-delete-matrix.md`，登记 24 表、全部操作列和 13 个删除接口。

## 2. 确定性全栈自动化基线

按顺序执行，保留每条命令的通过、失败、耗时和阻断原因。失败先归因，不在本任务内修改业务代码。

```bash
make contract-check
make test-unit
make test-integration
make e2e
make lint
make typecheck
make build
```

- [ ] 合同比较通过，或记录 OpenAPI、运行时 Schema、前端生成类型的精确差异。
- [ ] 后端单元、前端 Vitest 和视觉合同测试完成。
- [ ] PostgreSQL 集成测试完成，重点记录状态机、删除守卫、外部边界和审计结果。
- [ ] 现有 Playwright 主流程完成；共享 setup 阻断时记录具体请求、错误码和受影响用例，不把未进入页面的用例算作通过。
- [ ] lint、类型检查和构建只作为交付完整性证据；其失败不自动归类为功能缺陷，需说明影响。

全部分项完成且没有未解释失败后，可选运行聚合命令确认没有遗漏：

```bash
make verify
```

若聚合命令只重复已执行检查且没有新增证据，则跳过并在报告说明。

## 3. 浏览器会话与环境基线

- [ ] 启动或确认开发全栈；服务启动不算通过，必须实际打开页面并完成 API 请求。
- [ ] 使用 `playwright-cli` 建立 `full-admin`、`full-engineer` 和 `full-anonymous` 三个隔离会话。
- [ ] 验证目标 URL、页面版本、登录入口、管理员/工程师角色和会话失效。
- [ ] 记录初始 console、失败请求、数据库迁移和关键服务健康状态。
- [ ] 认证状态只保存在未跟踪位置；报告不包含密码、Cookie、Token、API Key 或敏感 Header。

## 4. W0：认证、权限和基础配置

- [ ] 未登录访问、错误登录、正确登录、刷新、退出、会话过期和浏览器后退。
- [ ] 管理员与工程师的导航、直达 URL、页面挂载和真实请求权限。
- [ ] 用户创建、编辑、停用、最后管理员保护、删除、批量操作和审计。
- [ ] 平台类型、平台、Prompt/revision/绑定、发布账号的创建、编辑、启停、冲突和删除。
- [ ] AI 渠道、敏感 Header、模型发现、连接测试、启停、变更后测试失效和删除。
- [ ] 配置完成后记录对象 ID、revision、状态和审计事件。

## 5. W1–W2：产品、事实、内容任务与审核

- [ ] 创建测试产品和 PUBLIC/RESTRICTED 事实，完成提交、退回、修订、批准和不可变检查。
- [ ] 验证 DRAFT、CHANGES_REQUESTED、APPROVED、RETIRED 的允许与禁止动作。
- [ ] 创建内容任务，核对平台 Prompt、revision、模型选择和可用原因。
- [ ] 分别完成一次真实网络边界的 AI 草稿、人工首稿，以及适用时的一次自然化。
- [ ] 验证非法 JSON、空字段、禁用模型、受限事实、重复点击、迟到结果和重试不会伪造成功。
- [ ] 完成内容提交、退回、修订、批准、历史与生成快照检查。

## 6. W3–W5：发布、异常修复与 GEO

- [ ] 使用批准内容和匹配账号创建发布记录，按合法状态推进并验证非法跳转。
- [ ] 验证跨平台账号、禁用账号、非法域名、重复登记、换账号绕过和旧页面重放。
- [ ] 验证证据上传、绑定、失败、独占清理和审计。
- [ ] 产生适用的发布异常，验证唯一 OPEN 待办、修复任务和显式解决相互独立。
- [ ] 建立 GEO 问题/查询、人工观测、文章结果和一次纠错链。
- [ ] 验证原观测保留、链尾口径、未知值、聚合指标、筛选、分页、打印和审计一致。

## 7. W6：恢复性、并发与跨页面一致性

- [ ] 对 AI 创建、审核、发布登记、GEO 新增和删除代表对象执行双击、Enter、刷新、后退和关闭弹窗。
- [ ] 使用两个标签页验证 revision 冲突和状态变化后的旧页面操作。
- [ ] 验证 401、403、409、422、429、5xx 和网络中断的反馈、输入保留和恢复入口。
- [ ] 对比列表、详情、总览指标、审计、重新登录和数据库最终状态。
- [ ] 失败场景可使用浏览器网络中断模拟 UI 恢复，但不得据此宣称服务端业务逻辑通过。

## 8. 重点专项：24 表与全部操作列

- [ ] 逐表执行桌面主视口，记录查询、状态、字段、操作、空/失败和详情返回。
- [ ] 逐表核对操作列的显示、禁用、菜单、确认、执行、失败恢复和 query invalidation。
- [ ] 对服务端未返回 `available_actions` 或角色无权的动作，验证 UI 不误导且直接请求被拒绝。
- [ ] 验证状态在菜单/弹窗打开后变化时，旧页面操作不会越过最新门禁。
- [ ] 运行现有长文本与几何断言，并用真实页面检查固定列、内部滚动、行高和 Tooltip。
- [ ] 将发现按共享根因归组，不把同一组件或合同问题机械登记为 24 个无关联缺陷。

## 9. 重点专项：13 个删除接口

以下对象逐项完成 UI、HTTP、服务、数据库与审计证据：

- [ ] 用户。
- [ ] 产品。
- [ ] 事实版本。
- [ ] 内容任务。
- [ ] 发布账号。
- [ ] 发布记录。
- [ ] GEO 人工观测完整更正链。
- [ ] 平台类型。
- [ ] 平台 Prompt。
- [ ] 平台。
- [ ] AI 渠道。
- [ ] AI 请求 Header。
- [ ] AI 模型。

每项至少验证：

- [ ] 允许删除的最小合法对象。
- [ ] 状态不允许、关联占用或受保护历史。
- [ ] 未登录、工程师、管理员及适用业务角色。
- [ ] CSRF、目标不存在、旧 revision、直接重复 DELETE 和并发状态变化。
- [ ] 确认文案、取消、加载、防重复、成功和结构化失败。
- [ ] 数据库原子性、关联记录、附件清理调度、审计和页面缓存一致性。

## 10. 全项目 UI/UX、响应式与可访问性

- [ ] 全部路由在 `1440×1000` 完成信息架构、功能状态和 console/requests 检查。
- [ ] 关键页面在 `375×900` 检查导航、表格、表单、抽屉、弹窗和触控目标。
- [ ] 代表页面在 `1024×768` 与 `320×800` 检查布局临界点。
- [ ] 登录、配置、数据列表、编辑审核、发布/GEO 五类页面执行真实 200% 缩放。
- [ ] 浅色、深色、跟随系统检查文字、边界、状态、Tooltip、表格、图表和焦点。
- [ ] 仅键盘完成登录、导航、表格操作菜单、关键表单、确认/取消和焦点恢复。
- [ ] 检查 reduced-motion、普通文字 4.5:1、非文字界面 3:1 和非颜色单一表达。
- [ ] 截图只用于基线或缺陷证据；没有状态、网络或副作用证据的截图不作为功能通过。

## 11. 缺陷记录与产品判定

- [ ] 创建 `research/findings.md`，每个缺陷包含 ID、类型、严重程度、角色、前置对象、最短步骤、期望/实际、错误码、证据和影响范围。
- [ ] 对需求歧义单列产品决策项，不混入实现缺陷。
- [ ] 每个 P0/P1 由源码、合同、真实请求或数据库证据复核，避免仅凭视觉猜测。
- [ ] 按共同根因整理后续修复任务建议，明确受影响页面和最小权威修复位置。
- [ ] 不修改业务代码、现有测试或权威文档；测试设施阻断也只记录并请求后续任务。

## 12. 最终报告与退出门禁

- [ ] 创建 `report.md`，包含结论、环境、覆盖、风险矩阵、状态机、W0–W6、表格/操作/删除专项、UI/UX、缺陷分布、阻断和残余风险。
- [ ] 所有计划项使用 `PASS/FAIL/BLOCKED/NOT_RUN/NOT_APPLICABLE`，未执行不得折算通过。
- [ ] P0 风险全部执行；任何 P0 失败或无法验证时结论为阻断。
- [ ] 主业务链路、24 表、操作列和 13 删除接口存在未解释的 P1/BLOCKED 时，不给出全项目通过结论。
- [ ] 报告列出全部测试对象 ID、保留/清理状态、证据路径和未覆盖项。
- [ ] 报告给出后续修复任务建议，但不创建、修改或提交业务修复。
- [ ] 运行 `git diff --check`，确认本任务只包含规划、测试矩阵、缺陷和报告产物，没有业务代码改动。

## 13. 回滚与清理

- [ ] 关闭命名浏览器会话并清理本地认证状态。
- [ ] 清理允许删除的临时配置和测试对象；受保护历史按产品合同保留并在报告登记。
- [ ] 若执行中断，保留已完成用例状态和证据，不把部分结果包装成全量通过。
- [ ] 本任务不提交、不推送、不部署、不归档；完成后先向用户展示报告和后续修复任务建议。
